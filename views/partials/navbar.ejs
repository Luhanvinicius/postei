<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <a class="navbar-brand" href="/<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">
            <span class="text-gradient">postei.pro</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="navbar-nav ms-auto d-flex align-items-center">
                <% if (typeof user !== 'undefined' && user) { %>
                    <% if (user.role === 'admin') { %>
                        <a class="nav-link" href="/admin/dashboard<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">Admin</a>
                        <a class="nav-link" href="/admin/users<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">Usuários</a>
                        <a class="nav-link" href="/admin/videos<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">Vídeos</a>
                        <a class="nav-link" href="/admin/invoices<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">Faturas</a>
                    <% } else { %>
                        <a class="nav-link" href="/user/dashboard<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">Dashboard</a>
                        <a class="nav-link" href="/user/accounts<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">Contas</a>
                        <a class="nav-link" href="/user/videos<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">Vídeos</a>
                        <a class="nav-link" href="/user/scheduled<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">Agendados</a>
                        <a class="nav-link" href="/user/published<%= typeof token !== 'undefined' && token ? '?token=' + token : '' %>">Publicados</a>
                    <% } %>
                    <%- include('profile-menu') %>
                <% } else { %>
                    <a class="nav-link" href="/#features">Recursos</a>
                    <a class="nav-link" href="/#pricing">Planos</a>
                    <a class="nav-link" href="/auth/login">Login</a>
                    <a class="nav-link btn btn-primary ms-2" href="/auth/register">Começar</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>
