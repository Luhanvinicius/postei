<%- include('../layouts/main', { title: 'Vídeos Agendados', user: user, body: `
<div class="row">
    <div class="col-12">
        <h1 class="mb-4">📅 Vídeos Agendados</h1>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <% if (schedules && schedules.length > 0) { %>
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>Vídeo</th>
                                    <th>Título</th>
                                    <th>Agendado para</th>
                                    <th>Status</th>
                                    <th>Ações</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% schedules.forEach(schedule => { %>
                                    <tr>
                                        <td><%= schedule.video_path.split(/[\\/]/).pop() %></td>
                                        <td><%= schedule.title || 'Sem título' %></td>
                                        <td>
                                            <%= new Date(schedule.scheduled_time).toLocaleString('pt-BR') %>
                                        </td>
                                        <td>
                                            <% if (schedule.status === 'pending') { %>
                                                <span class="badge bg-warning">Pendente</span>
                                            <% } else if (schedule.status === 'processing') { %>
                                                <span class="badge bg-info">Processando</span>
                                            <% } else if (schedule.status === 'completed') { %>
                                                <span class="badge bg-success">Publicado</span>
                                            <% } else if (schedule.status === 'failed') { %>
                                                <span class="badge bg-danger">Falhou</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <% if (schedule.status === 'completed' && schedule.video_id) { %>
                                                <a href="https://www.youtube.com/watch?v=<%= schedule.video_id %>" 
                                                   target="_blank" class="btn btn-sm btn-success">
                                                    Ver no YouTube
                                                </a>
                                            <% } else if (schedule.status === 'failed') { %>
                                                <small class="text-danger"><%= schedule.error || 'Erro desconhecido' %></small>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                <% } else { %>
                    <p class="text-muted">Nenhum vídeo agendado.</p>
                <% } %>
            </div>
        </div>
    </div>
</div>
` }) %>
