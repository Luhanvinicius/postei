<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerenciar Vídeos - postei.pro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .lock-container {
            min-height: 60vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 3rem 0;
        }
        .lock-card {
            max-width: 600px;
            text-align: center;
            padding: 3rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        .lock-icon {
            font-size: 5rem;
            color: #9333ea;
            margin-bottom: 1.5rem;
        }
        .pricing-card {
            border: 2px solid rgba(139, 43, 226, 0.2);
            border-radius: 15px;
            padding: 2rem;
            text-align: center;
            transition: all 0.3s;
            height: 100%;
            background: white;
            margin-top: 2rem;
        }
        .pricing-card:hover {
            border-color: #9333ea;
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(139, 43, 226, 0.3);
        }
        .price {
            font-size: 2.5rem;
            font-weight: bold;
            background: linear-gradient(135deg, #8b2be2 0%, #9333ea 50%, #ec4899 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .btn-gradient {
            background: linear-gradient(135deg, #8b2be2 0%, #9333ea 50%, #ec4899 100%);
            border: none;
            color: white;
            padding: 12px 30px;
            border-radius: 25px;
            font-weight: 600;
            transition: all 0.3s;
            box-shadow: 0 5px 15px rgba(139, 43, 226, 0.3);
        }
        .btn-gradient:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(139, 43, 226, 0.4);
            color: white;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">📹 postei.pro</a>
            <div class="navbar-nav ms-auto d-flex align-items-center">
                <a class="nav-link" href="/user/dashboard">Dashboard</a>
                <a class="nav-link" href="/user/accounts">Vincular Contas</a>
                <a class="nav-link active" href="/user/videos">Vídeos</a>
                <a class="nav-link" href="/user/scheduled">Agendados</a>
                <a class="nav-link" href="/user/published">Publicados</a>
                <%- include('../partials/profile-menu') %>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        <div class="lock-container">
            <div class="lock-card card">
                <div class="card-body">
                    <div class="lock-icon">
                        <i class="bi bi-lock-fill"></i>
                    </div>
                    <h2 class="mb-3">🔒 Funcionalidade Bloqueada</h2>
                    <p class="lead text-muted mb-4">
                        Para usar a funcionalidade de gerenciamento de vídeos, você precisa adquirir um plano ativo.
                    </p>
                    <p class="text-muted mb-4">
                        Com um plano ativo, você poderá:
                    </p>
                    <ul class="list-unstyled text-start mb-4" style="max-width: 400px; margin: 0 auto;">
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i> Gerenciar e organizar seus vídeos</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i> Gerar conteúdo com IA automaticamente</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i> Agendar publicações</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i> Publicar vídeos diretamente no YouTube</li>
                    </ul>
                    <a href="/user/plans" class="btn btn-gradient btn-lg">
                        <i class="bi bi-credit-card me-2"></i>Escolher e Adquirir Plano
                    </a>
                </div>
            </div>
        </div>

        <!-- Mostrar planos disponíveis -->
        <div class="row mb-5">
            <div class="col-12">
                <h3 class="text-center mb-4">📦 Planos Disponíveis</h3>
            </div>
            <% plans.forEach((plan, index) => { %>
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="pricing-card <%= index === 1 ? 'border-primary' : '' %>">
                    <% if (index === 1) { %>
                    <div class="badge bg-primary mb-2" style="background: linear-gradient(135deg, #8b2be2 0%, #9333ea 50%, #ec4899 100%) !important;">Mais Popular</div>
                    <% } %>
                    <h4 class="fw-bold mb-3"><%= plan.name %></h4>
                    <div class="price mb-3">R$ <%= parseFloat(plan.price).toFixed(2) %><span class="fs-6">/mês</span></div>
                    <ul class="list-unstyled mb-4 text-start">
                        <% 
                        let features = [];
                        try {
                            features = JSON.parse(plan.features || '[]');
                        } catch (e) {
                            features = plan.features ? plan.features.split(',') : [];
                        }
                        features.forEach(feature => { %>
                            <li class="mb-2"><i class="bi bi-check text-success me-2"></i><%= feature %></li>
                        <% }); %>
                        <% if (plan.max_videos) { %>
                            <li class="mb-2"><i class="bi bi-check text-success me-2"></i>Até <%= plan.max_videos %> vídeos/mês</li>
                        <% } else { %>
                            <li class="mb-2"><i class="bi bi-check text-success me-2"></i>Vídeos ilimitados</li>
                        <% } %>
                        <% if (plan.max_channels) { %>
                            <li class="mb-2"><i class="bi bi-check text-success me-2"></i>Até <%= plan.max_channels %> canal(is)</li>
                        <% } else { %>
                            <li class="mb-2"><i class="bi bi-check text-success me-2"></i>Canais ilimitados</li>
                        <% } %>
                    </ul>
                    <a href="/payment/checkout/<%= plan.slug %>" class="btn btn-gradient w-100" style="text-decoration: none; color: white;">Escolher Plano</a>
                </div>
            </div>
            <% }); %>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>



